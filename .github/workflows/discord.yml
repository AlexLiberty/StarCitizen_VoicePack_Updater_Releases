name: Notify Discord on Release

on:
  release:
    types: [published]

jobs:
  notify-discord:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Notification
        env:
          DISCORD_WEBHOOK: ${{ secrets.USFDISCORDHOOK }}
          RELEASE_NAME: ${{ github.event.release.name }}
          RELEASE_BODY: ${{ github.event.release.body }}
          RELEASE_URL: ${{ github.event.release.html_url }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          # Витягуємо перше зображення з Markdown (формат ![alt](url))
          IMAGE_URL=$(echo "$RELEASE_BODY" | grep -oP '!\[.*?\]\(\K.*?(?=\))' | head -n 1)

          # Очищаємо тіло від всіх Markdown-зображень
          CLEAN_DESCRIPTION=$(echo "$RELEASE_BODY" | sed -E 's/!\[.*?\]\(.*?\)//g')

          # Формуємо опис: заголовок, версія, зображення (якщо є), потім опис
          if [ -n "$IMAGE_URL" ]; then
            FULL_DESCRIPTION="🚀 Новий реліз: $RELEASE_NAME\nВерсія: $RELEASE_TAG\n\n![image]($IMAGE_URL)\n\n$CLEAN_DESCRIPTION"
          else
            FULL_DESCRIPTION="🚀 Новий реліз: $RELEASE_NAME\nВерсія: $RELEASE_TAG\n\n$CLEAN_DESCRIPTION"
          fi

          # Створюємо JSON payload
          EMBED=$(jq -n \
            --arg description "$FULL_DESCRIPTION" \
            --arg url "$RELEASE_URL" \
            --argjson color 5814783 \
            '{embeds: [{
              description: $description,
              url: $url,
              color: $color
            }]}' )

          # Відправляємо до Discord
          curl -X POST -H "Content-Type: application/json" \
            -d "$EMBED" \
            "$DISCORD_WEBHOOK"
