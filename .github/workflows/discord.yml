name: Notify Discord on Release

on:
  release:
    types: [published]

jobs:
  notify-discord:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Notification
        env:
          DISCORD_WEBHOOK: ${{ secrets.USFDISCORDHOOK }}
          RELEASE_NAME: ${{ github.event.release.name }}
          RELEASE_BODY: ${{ github.event.release.body }}
          RELEASE_URL: ${{ github.event.release.html_url }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          # Витягуємо перше посилання на картинку з Markdown у тілі релізу
          IMAGE_URL=$(echo "$RELEASE_BODY" | grep -oP '!\[.*?\]\(\K.*?(?=\))' | head -n 1)

          # Видаляємо всі Markdown картинки, щоб не дублювались
          CLEAN_DESCRIPTION=$(echo "$RELEASE_BODY" | sed -E 's/!\[.*?\]\(.*?\)//g')

          # Формуємо заголовок і версію
          HEADER="🚀 Новий реліз: $RELEASE_NAME\nВерсія: $RELEASE_TAG"

          # Якщо є картинка, вставляємо її Markdown під заголовком
          if [ -n "$IMAGE_URL" ]; then
            HEADER="$HEADER\n\n![image]($IMAGE_URL)"
          fi

          # Об'єднуємо заголовок + картинку + опис
          FULL_DESCRIPTION="$HEADER\n\n$CLEAN_DESCRIPTION"

          # Формуємо JSON embed
          EMBED=$(jq -n \
            --arg description "$FULL_DESCRIPTION" \
            --arg url "$RELEASE_URL" \
            --argjson color 5814783 \
            '{embeds: [{
              description: $description,
              url: $url,
              color: $color
            }]}')

          # Відправляємо запит до Discord
          curl -X POST -H "Content-Type: application/json" \
            -d "$EMBED" \
            "$DISCORD_WEBHOOK"
